<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ourearth">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OurEarth">
        <activity android:name=".progress"></activity>
        <activity android:name=".workshop" />
        <activity android:name=".friends_socials" />
        <activity android:name=".correct_petitions_page" />
        <activity android:name=".animalgamepage" />
        <activity android:name=".gamepage" />
        <activity android:name=".homepage" />
        <activity android:name=".login_registration_page" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>